# TIM0000 is realtek BT adapter RTL8723 on ECS

[global]
module_name = bluetooth
library_name = bluetooth.default.so

[device]
bus = acpi
desc = TIM0000

[script]
filename = rc.hci-def_attach
one_shot = 1
